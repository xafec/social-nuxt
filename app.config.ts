export default defineAppConfig({
  ui: {
    gray: "neutral",
    primary: "green",
    notification: {
      default: {
        timeout: 2000,
      },
    },
    icons: {
      dynamic: true,
    },
  },
});
